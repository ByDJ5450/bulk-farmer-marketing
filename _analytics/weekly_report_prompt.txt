주간 채널 성과 리포트를 생성한다. 대화 상대는 없다. 질문하지 말고 끝까지 수행한 뒤 결과 파일을 남긴다.

## 이 턴 안에서 끝내야 한다 (2026-08-03 실제로 실패한 지점)

이건 헤드리스 1회성 실행이다. **네 턴이 끝나는 순간 프로세스가 죽는다.**
나중에 알림을 받거나, 다음 턴에 이어서 하거나, 결과를 확인할 기회는 없다.

절대 하지 말 것:
- 서브에이전트에 위임하고 끝내기 (Agent/Task 도구는 쓰지 않는다)
- `&` 나 백그라운드 실행으로 넘기고 끝내기
- "완료되면 보고하겠습니다" 같은 말로 마무리하기 — 보고할 다음 턴이 없다

2026-08-03 실행이 정확히 이렇게 실패했다. "백그라운드에서 performance-analyst 에이전트가
실행 중입니다. 완료되면 알림이 올 것"이라고 말하고 끝냈고, 리포트는 만들어지지 않았다.
종료 코드만 0이었다.

**리포트 파일을 실제로 Write 한 다음에 끝낸다.** 수집이 오래 걸려도 기다린다.
일부 채널 수집이 실패하면 그 채널을 "데이터 없음"으로 적고 나머지로 리포트를 완성한다.
빈손으로 끝내는 것보다 낫다.

절차:

1. `.claude/agents/performance-analyst.md`를 읽고 그 규칙을 그대로 따른다.
   특히 --flat-playlist 금지, video ID 기준 중복 제거, 중앙값 우선 판단, n 병기 규칙을 지킨다.

2. `_analytics/` 에서 가장 최근 리포트를 읽는다. 지난번 "다음 주 기획 인풋"이 실행됐는지
   이번 데이터로 대조한다.

3. 유튜브 전수 수집 (롱폼 + 쇼츠). PATH에 yt-dlp가 있어야 한다:
   export PATH="/Library/Frameworks/Python.framework/Versions/3.14/bin:$PATH"

4-1. 스레드 수집 (Threads API). `~/.config/bulkfarmer/meta.env` 에 자격증명이 있다.
     게시물 목록 + 게시물별 지표(views/likes/replies/reposts/quotes) + 계정 지표.
     상세 절차는 performance-analyst.md "스레드" 섹션 참조.
     **토큰 만료 점검 필수** — THREADS_TOKEN_EXPIRES 가 7일 이내면 갱신하고 결과를 알린다.
     토큰 값은 어떤 출력에도 찍지 않는다.

4. 틱톡 수집 (--ignore-errors 필수). 성공/실패 개수를 리포트에 명시한다.
   실패율 60% 초과 시 `yt-dlp -U` 후 1회 재시도하고, 그래도 안 되면 "틱톡 수집 실패"로 기록한다.

4-2. 인스타그램 수집 (Instagram Graph API). 자격증명은 같은 meta.env.
     미디어 목록 + 게시물별 지표(reach/saved/shares/likes/comments/profile_visits)
     + 계정 지표(reach/profile_views/website_clicks).
     media_type(IMAGE/VIDEO/CAROUSEL_ALBUM)별로 나눠서 본다 — 카드뉴스는 CAROUSEL_ALBUM.
     **website_clicks·profile_visits가 전환에 가장 가까운 지표다.** 반드시 리포트에 넣는다.
     IG_TOKEN_EXPIRES 7일 이내면 갱신한다.

5. `_analytics/manual_input.md`을 읽는다. 표가 2개다 — 전환 지표, 네이버 블로그.
   비어 있으면 추정하지 말고 "데이터 없음"으로 명시한다.
   전환 지표가 2주 이상 연속으로 비어 있으면 그 사실을 **요약 맨 위**에 올린다.
   조회수만 보고 판단하고 있다는 뜻이고, 그게 지금 이 시스템의 가장 큰 구멍이다.

5-0. 네이버 블로그. 발행 API도 통계 API도 없어 자동 수집이 불가능하다.
     `memory/project_blog_published.md`의 발행 이력과 manual_input 2번 표를 대조해
     발행은 했는데 통계가 안 들어온 주가 있으면 지적한다.
     **검색 유입 비중**이 이 채널의 유일한 성공 지표다. 총 조회수로 판단하지 않는다.

5-0-1. 레퍼런스 채널 갱신 (필수). `_context/competitor_reference.md` 를 읽고
     기재된 채널들을 yt-dlp로 다시 수집한다 (`--playlist-end 45`, flat-playlist 금지).

     - 각 채널의 쇼츠·롱폼 중앙값을 우리 것과 대조해 배율을 갱신한다
     - **지난 수집 이후 새로 올라온 상위 영상**(그 채널 중앙값의 3배 이상)을 찾는다.
       제목 패턴에 새로운 공식이 보이면 문서의 "가져올 것"에 추가한다
     - WebSearch로 **새로 뜬 채널**을 찾는다. 검색어는 그때그때 바꾼다 —
       마른 체형 벌크업 / 멸치 탈출 / 체중 증량 / 저체중 근육 등.
       구독 1만 이상이고 우리 니치면 문서에 추가한다
     - 문서를 실제로 Edit 한다. 리포트에만 적고 끝내지 않는다

     추정치를 사실처럼 쓰지 않는다. 수집 실패한 채널은 실패로 적는다.

5-1. 벤치마킹 (필수). WebSearch로 **오늘 시점** 국내 피트니스·벌크업 상위권
     인스타그램 계정을 조사한다. 피드(훅 유형·포맷 비중·캡션 구조)와
     스토리 운영(하루 개수·용도)을 본다.
     리포트에는 **그들이 하고 우리가 안 하는 것만** 적는다.
     계정명과 확인 날짜를 명시하고, 추정치를 사실처럼 쓰지 않는다.

5-2. 지난 리포트의 "개선 과제"를 다시 읽는다. 실행되지 않은 항목을
     주차와 함께 적는다. 3주 연속 방치된 항목은 요약 맨 위로 올린다.

6. 리포트를 `_analytics/{YYYY-MM-DD}_channel_report.md`로 저장한다.
   형식은 performance-analyst.md의 "리포트 형식" 섹션(7개 절)을 따른다.
   **어투는 냉정하게.** 위로·완충 문장 금지. 성과가 나빴으면 첫 줄에 그렇게 쓴다.
   "아쉽지만", "그래도", "잘 하고 계십니다" 같은 표현을 쓰지 않는다.

7. 전략 문서(`CLAUDE.md`, `_context/youtube_strategy.md`)의 전제 중 이번 데이터와
   어긋나는 것이 있으면 리포트 3번 섹션에 정리한다. 문서를 직접 수정하지는 않는다.

8. 텔레그램으로 요약을 보낸다. 자격증명은 `~/.config/bulkfarmer/telegram.env`에 있다.

   source ~/.config/bulkfarmer/telegram.env
   curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
     -H "Content-Type: application/json" \
     -d "$(python3 -c '...')"     # chat_id, parse_mode=HTML, text 를 JSON으로 구성

   메시지 형식 (600자 이내, 폰에서 읽는다):
     <b>📊 주간 성과 리포트</b>
     {날짜}
     · 유튜브 쇼츠 중앙값 {값} ({전주 대비 ±%})
     · 유튜브 롱폼 중앙값 {값} ({전주 대비 ±%})
     · 틱톡 {수집 성공 n개} / 인스타·스레드 {데이터 유무}
     <b>이번 주 발견</b>
     {가장 중요한 것 1~2개, 각 한 줄}
     <b>방치된 과제</b>
     {몇 주째 실행 안 된 항목 — 없으면 "없음"}
     <b>이번 주 할 것</b>
     {1~2개, 실행 단위}
     전문: _analytics/{파일명}

   토큰 노출 금지: 로그나 출력에 TELEGRAM_BOT_TOKEN 값을 절대 찍지 않는다.

9. 5~8단계 중 어디서든 실패하면, 실패 사실을 텔레그램으로 보낸다.
   조용히 끝나면 안 된다. 예: "⚠️ 주간 리포트 실패 — yt-dlp 수집 단계. 로그: _analytics/cron.err.log"

주의:
- 중간 스크립트 파일(.py, .sh)을 만들지 않는다. Bash 인라인으로 실행한다.
- 지난주 대비 변화가 없으면 "변화 없음"이라고 쓴다. 억지로 발견을 만들지 않는다.
- 성과가 하락했으면 하락했다고 쓴다.
- 마지막 줄에 한 줄 요약을 출력한다: "리포트 저장: {경로} / 핵심 발견: {한 줄}"
